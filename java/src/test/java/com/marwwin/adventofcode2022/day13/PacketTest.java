package com.marwwin.adventofcode2022.day13;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

public class PacketTest {

  // @Test
  // public void shouldTakeAStringAsInputAndCreateAPacket() {
  //   Packet packet = new Packet("[1,1,3,1,1]");
  // }
// 
  // @Test
  // public void shouldDecodeList() {
  //   Packet packet = new Packet("[1,1,3,1,1]");
  //   assertEquals(packet.getIndex(0).getNumber(), 1);
  //   assertEquals(packet.getIndex(2).getNumber(), 3);
  //   assertEquals(packet.getPacket().size(), 5);
  // }
// 
  // @Test
  // public void shouldDecodeANestedList() {
  //   Packet packet = new Packet("[[1],4]");
  //   assertEquals(packet.getIndex(0).isPacket(), true);
  //   // assertEquals(packet.getIndex(2), 3);
  //   // assertEquals(packet.getPacket().size(), 5);
  // }
}
